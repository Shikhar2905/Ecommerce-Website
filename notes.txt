1. We should give heighth to div , else the content or image might not be visible
2. If we want to align two items horizontally and if they are inside "div" use "display-flex" property
3. rem - root em - it indicates an elements font size relative to the size of the root element